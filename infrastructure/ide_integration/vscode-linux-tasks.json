{
  "version": "2.0.0",
  "tasks": [
  {
    "label": "ATE desktop build",
    "type": "shell",
    "command": "clear; BUILD_DIR="../build/ATE"; mkdir -p $BUILD_DIR; cmake -B$BUILD_DIR -S./ ; make -C $BUILD_DIR;",
    "problemMatcher": [
      "$gcc"
    ],
    "group": {
      "kind": "build",
      "isDefault": true
      }
    }
  ]
}
